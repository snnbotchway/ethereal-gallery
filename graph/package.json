{
  "name": "ethereal-gallery",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ethereal-gallery",
    "create-local": "graph create --node http://localhost:8020/ ethereal-gallery",
    "remove-local": "graph remove --node http://localhost:8020/ ethereal-gallery",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ethereal-gallery",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.50.1",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
